import React from 'react';
import Navbar from './navbar/Navbar';

const Header = () => {
    return (
        <header className="col-">
            <Navbar />
        </header>
    );
}

export default Header;